# Frontrow — Architecture

**Lifecycle step:** 5 of 17 · **Locked:** 2026-09-15

## 1. System diagram

```mermaid
flowchart LR
  subgraph Browser
    W[Next.js pages<br/>SeatMap · Checkout · Organiser]
    RZ[Razorpay Checkout modal]
  end
  subgraph Vercel
    WEB[web · frontrow<br/>Next.js, static + SSR<br/>rewrites /api/* →]
    API[api · frontrow-api<br/>FastAPI, bom1<br/>REST + SSE + webhooks]
  end
  PG[(Neon Postgres<br/>users · venues · seats · events<br/>showtimes · orders · tickets)]
  RD[(Upstash Redis<br/>hold:* TTL · holds:{st} hash · st:{st}:v)]
  RP[Razorpay]
  RS[Resend · v2]
  BL[Vercel Blob · v2]

  W -->|same-origin /api/*| WEB --> API
  W -.->|SSE /showtimes/{id}/stream · v2| API
  RZ -->|payment| RP -->|webhook payment.captured| API
  API --> PG
  API --> RD
  API -->|refund · order.create| RP
  API -->|ticket email| RS
  API -->|posters| BL
```

## 2. Boundaries
| Component | Owns | Never does |
|---|---|---|
| **web/** | Rendering, seat-map interaction, countdown clock, Razorpay modal, URL state | Touch the DB or Redis; compute prices; trust client totals |
| **api/** | Auth, holds, orders, tickets, organiser CRUD, webhooks, SSE, seed | Render HTML; hold WebSockets |
| **Redis** | Holds (TTL) and version counters — *fast, disposable* | Be the source of truth for anything sold |
| **Postgres** | Everything durable; the `UNIQUE (showtime_id, seat_id)` guard | Hold-expiry timing |
| **Razorpay** | Card/UPI capture, refunds | Decide whether a seat is sellable (the webhook handler does) |

Contract: `api/openapi.json` (generated by FastAPI) → `web/src/lib/api-types.ts` via `pnpm gen:api`. The web client (`web/src/lib/api.ts`) is a typed `fetch` that forwards cookies and trusts only the `{ error: { code, message } }` envelope.

## 3. api/ layout
```
api/app/
  main.py            app factory, CORS off (same-origin via rewrite), request-id middleware
  config.py          pydantic-settings
  db.py  redis.py    engines; redis-py asyncio client, Lua scripts registered at startup
  models/            SQLAlchemy 2.0: user, session, venue, seat, event, showtime, price_tier, order, ticket, webhook_event (+ v3: waitlist, transfer)
  schemas/           pydantic request/response models (= the OpenAPI contract)
  routers/           auth, events, showtimes (seats, holds, stream), orders, tickets, organiser, webhooks, checkin
  services/
    layouts.py       template generators → layout JSON + seat rows
    holds.py         Lua acquire/release, snapshot, version
    orders.py        totals, Razorpay order, confirm transaction, paid-after-expiry
    tickets.py       token sign/verify
    stream.py        SSE generator
  seed/              content (venues, events, photos CREDITS.md), seed.py
tests/               the tests listed in 04 §11 only
```

## 4. web/ layout
```
web/src/
  app/(site)/        page.tsx · events/ · events/[slug]/ · book/[showtimeId]/ · checkout/[orderId]/ · tickets/[orderId]/ · my-tickets/
  app/(auth)/        sign-in · sign-up
  app/(organiser)/   organiser/venues · events · showtimes · bookings/[showtimeId] · dashboard (v2) · checkin/[showtimeId] (v2)
  components/seat-map/   SeatMap.tsx · Section.tsx · Seat.tsx · useSeatState.ts · useCountdown.ts · viewbox.ts
  components/landing/    (exists)
  components/ui/
  lib/api.ts · api-types.ts (generated) · session.ts
```

## 5. Deployment topology
- Two Vercel projects per repo (as Tripsmith): `frontrow` (root `web/`) and `frontrow-api` (root `api/`, FastAPI preset, `bom1`). `web/next.config.ts` rewrites `/api/:path*` → `API_URL`, so the browser only ever talks to `frontrow.virajdomadia.com`; cookies are first-party; no CORS.
- SSE route sets `maxDuration: 300` in `api/vercel.json`; every other route 30 s.
- Neon: one project, `main` branch = production, a `dev` branch for local. Upstash: one Redis, region Mumbai. Razorpay webhook URL → `https://frontrow.virajdomadia.com/api/webhooks/razorpay`.
- Previews: Vercel previews per PR for `web/` (API previews are SSO-gated, so previews point at the production API — acceptable for a portfolio; seeded data is regenerated relative to "now").
- CI (GitHub Actions): `web` = pnpm typecheck + build; `api` = ruff + pytest with `postgres:17` and `redis:7` services. That is all.

## 6. Request paths worth drawing
**Hold:** browser → `POST /api/showtimes/7/holds` → rewrite → FastAPI → (Postgres: sold seats for showtime 7, cached 5 s) → Redis Lua → 200 `{expires_at}` / 409 `{taken}`.
**Pay:** browser → Razorpay modal → Razorpay → webhook → FastAPI → Postgres tx (tickets, order) → Redis `DEL`/`INCR` → (v2) Resend. In parallel the browser calls `POST /orders/{id}/verify`, which confirms first in the common case; both paths are idempotent.
**Live (v2):** browser `EventSource` → FastAPI generator → Redis `GET v` each second / `HGETALL` on change → SSE events.

## 7. Security notes (the short list)
Cookie session HttpOnly + SameSite=Lax; argon2 passwords; Razorpay webhook HMAC + event-id dedupe; ticket token HMAC; server-side totals only; organiser routes check ownership; rate limit holds per session (30/min) to stop a script grabbing a hall; no PII in logs.
